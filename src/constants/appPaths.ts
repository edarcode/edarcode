const APP = "/";
const PROJECTS = `${APP}projects`;
const STUDIES = `${APP}studies`;
const UTILITIES = `${APP}utils`;

export const APP_PATH = {
	index: APP,
	projects: PROJECTS,
	studies: STUDIES,
	utilities: UTILITIES
};
